# Production build env (Vite). The Convex client URL is public by design — it is
# embedded in the browser bundle and is safe to commit (it is NOT an API key).
VITE_CONVEX_URL=https://notable-akita-913.convex.cloud
VITE_CONVEX_SITE_URL=https://notable-akita-913.convex.site

# Vapi public/web key — client-side by design (browser voice SDK), safe to commit.
VITE_VAPI_PUBLIC_KEY=eee9b688-0b49-46ed-979d-b2bd8606b8e5
