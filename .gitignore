node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# Local secrets — never committed
secrets.env
tsconfig.tsbuildinfo
*.tsbuildinfo
.vercel

# Env files: ignore all, but keep the public build config + example tracked
.env
.env.local
.env*.local
!.env.production
!.env.example
